// ARCHIVED: fetch_firestore_rules.js
// This utility fetched Firestore security rules using a service account and the
// Firebase Rules API. It has been archived in-place to avoid accidental use
// or exposing service-account logic. The original file is preserved in git
// history and can be restored if needed.

/* eslint-disable no-console */
console.log('fetch_firestore_rules.js archived — inert placeholder.');
process.exit(0);
