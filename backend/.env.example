# Example environment file for backend
# Copy to .env and adjust values before running

# Path to service account JSON (relative to backend folder or absolute)
SERVICE_ACCOUNT_PATH=serviceAccountKey.json

# Server port
PORT=5000

# ========================================
# Admin User Seed Configuration
# ========================================
# Use these to configure admin accounts when running:
#   node scripts/seed_admin_users.js
#
# Only users with both EMAIL and PASSWORD set will be created.
# You can omit any role you don't need.

# Administrator Account (Full access)
ADMIN_NAME=Admin User
ADMIN_EMAIL=admin@vserve.gov.ph
ADMIN_PASSWORD=Admin@2024!Secure
ADMIN_DEPARTMENT=IT Administration

# Viewer Account (Read-only access)
VIEWER_NAME=Viewer User
VIEWER_EMAIL=viewer@vserve.gov.ph
VIEWER_PASSWORD=Viewer@2024!Secure
VIEWER_DEPARTMENT=Building Permits

# SECURITY NOTES:
# - Passwords are hashed using bcrypt (memory-hard algorithm, work factor 12)
# - bcrypt provides protection against brute-force, dictionary, and rainbow table attacks
# - DELETE the .env file after seeding users for security!
# - Never commit .env to version control
