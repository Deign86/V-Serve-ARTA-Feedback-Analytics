{
  "name": "v-serve-arta-backend",
  "version": "0.1.0",
  "description": "Minimal backend scaffold for V-Serve ARTA Feedback (connects to Firestore via firebase-admin)",
  "main": "src/index.js",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "seed:admins": "node scripts/seed_admin_users.js",
    "generate:vapid": "node -e \"const webpush=require('web-push');console.log(JSON.stringify(webpush.generateVAPIDKeys()));\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "firebase-admin": "^11.9.0",
    "node-cron": "^3.0.2",
    "protobufjs": "^7.2.6",
    "web-push": "^3.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
